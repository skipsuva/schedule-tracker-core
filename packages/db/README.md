# Database Package

This package contains:
- Schema documentation
- Database migrations
- Row Level Security (RLS) policies

Migrations are stored in `supabase/migrations/` and managed via the Supabase CLI.
